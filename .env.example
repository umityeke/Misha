# Misha defaults — copy to .env only for local development.
# Never commit real credentials or database URLs.

MISHA_REMOTE_CONFIG_ENABLED=0
PORT=8080

# Optional remote configuration. Keep unset for the local-only default.
DATABASE_URL=
