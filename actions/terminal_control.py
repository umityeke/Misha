def terminal_control(parameters: dict, player=None):
    """Legacy arbitrary-shell entry point retained as an explicit fail-closed stub."""
    return (
        "Arbitrary terminal execution is disabled. "
        "Use a registered developer_tools action with fixed argv and sandbox policy."
    )
